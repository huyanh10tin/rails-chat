$('#my-conversations').append('<%= j render @conversations %>');
$('.conversation-body').last().after('<%= j render "conversation_body", conversations: @conversations %>');
<% if @conversations.next_page %>
  $('.pagination').replaceWith('<%= j paginate @conversations %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
