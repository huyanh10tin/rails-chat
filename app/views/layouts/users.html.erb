<%= content_for :content do %>
  <div class="columns">
    <div class="column is-3">
      <aside class="menu">
        <p class="menu-label">Profile</p>
        <ul class="menu-list">
          <li><%= link_to "Friends (#{current_user.friends.count})", friends_path, class: "navbar-item #{'is-active' if current_page? profile_path }" %></li>
          <li><%= link_to "Friend Requests", new_message_path, class: "navbar-item #{'is-active' if current_page? profile_path }"  %></li>
        </ul>
      </aside>
    </div>
  <div class="column">
    <%= yield %>
  </div>
  <% end %>
<%= render file: "layouts/application" %>
